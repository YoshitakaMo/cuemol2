name: Build test 2 (windows)

on:
  push:
    # branches-ignore:
    #   - '**'

###############################################

jobs:
  build_win_x64:
    name: "Windows intel msvc"
    runs-on: windows-2019
    steps:
    - uses: actions/checkout@v4

    - name: Add MSBuild to PATH
      uses: microsoft/setup-msbuild@v1.0.2

    - name: Install prerequisite
      run: |
        choco install mozillabuild
        

    # - name: Install prerequisite
    #   run: |
    #     choco install -r wget
    #     wget --progress=dot:mega https://ftp.mozilla.org/pub/mozilla/libraries/win32/MozillaBuildSetup-3.4.exe
    #     .\MozillaBuildSetup-3.4.exe /S
    #     dir
        
